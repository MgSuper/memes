import 'package:flutter/material.dart';

class MyCollectionScreen extends StatelessWidget {
  const MyCollectionScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('My Collection Screen'),
    );
  }
}
